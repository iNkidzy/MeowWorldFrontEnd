import DateTimeFormat = Intl.DateTimeFormat;

export interface Cat{
  name: string;
  type: string;
  color: string;
  price: number;
  createdDate: DateTimeFormat;
 // ownerId: number;
//  isComplete: boolean;

}
